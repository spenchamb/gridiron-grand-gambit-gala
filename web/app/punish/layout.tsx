import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "Punish Watch",
  description: "Simulated odds of finishing dead last.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
