# Normalize all text to LF in the repo and on checkout. The server runs Linux;
# CRLF in a shell script's shebang breaks it (bash\r: bad interpreter).
* text=auto eol=lf
*.png binary
*.jpg binary
*.ico binary
